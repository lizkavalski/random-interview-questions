import React from "react";

export default function RandomQuestions() {
  // let random = () => {
  //   random[Math.floor(Math.random() * items.length)];
  // };

  return (
    <div className="App">
      <button>Pick a Random Questions </button>
    </div>
  );
}
